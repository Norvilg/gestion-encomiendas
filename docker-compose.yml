version: '3.9'

services:
  web:
    build: .  # <-- El punto significa "busca el Dockerfile aquí mismo"
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
